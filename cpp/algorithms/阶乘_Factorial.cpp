//½×³Ë´úÂëÊµÏÖ 

/*
#include <iostream>
using namespace std;

unsigned long long factorial_Recursive(int n)
{
	if (n < 0) return 0;

	else if (n == 0 || n == 1) return 1;

	else return n * factorial_Recursive(n - 1);
}

unsigned long long factorial_Iterative(int n)
{
	unsigned long long result = 1;

	for (int i = 1; i <= n; i++)
	{
		result *= i;
	}

	return result;
}


int main()
{
	int number;
	std::cout << "Enter a number: ";
	std::cin >> number;

	unsigned long long result_Recursive = factorial_Recursive(number);
	std::cout << "Factorial_Recursive of " << number << " is " << result_Recursive << std::endl;

	unsigned long long result_Iteartive = factorial_Iterative(number);
	std::cout << "Factorial_Iteartive of " << number << " is " << result_Iteartive << std::endl;


	return 0;
}

*/


